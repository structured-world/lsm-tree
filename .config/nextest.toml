[profile.default]
slow-timeout = { period = "10s", terminate-after = 6 }

# Property tests (proptest) run 256 cases by default and can take 2+ minutes
[[profile.default.overrides]]
filter = "test(prop_)"
slow-timeout = { period = "30s", terminate-after = 8 }
